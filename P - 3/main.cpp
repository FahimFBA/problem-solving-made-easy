#include <bits/stdc++.h>

using namespace std;

int main()
{
    int variable;
    cin >> variable;
    if (variable % 3 == 0)
        cout << variable << " is divisible by 3" << endl;
    else
        cout << variable << " is not divisible by 3" << endl;
}
